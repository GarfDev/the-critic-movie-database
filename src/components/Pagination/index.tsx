interface Props {
  className?: string;
}

export const Pagination: React.FC<Props> = ({ className }) => {
  return <div className="flex border-black border-2 rounded-md p-2"></div>;
};

export default Pagination;
